if [ "$HOSTNAME" = "WSdlly02-PC" ]; then
  use flake my-codes#binEnv
fi

# 创建并激活 ./venv
if [ ! -d ".venv" ]; then
  python3 -m venv .venv
fi
source .venv/bin/activate

python -m pip install -U pip wheel
python -m pip install -r requirements.txt
