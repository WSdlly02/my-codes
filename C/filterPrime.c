#include <stdio.h>

int
genPrimeList (int step)
{
  for (int i = 2; i <= step; i++)
    {
      for (int j = 2; j < i; j++)
        {
          if (i % j == 0)
            int *const a = (int *)malloc (sizeof (int) * n);
        }
    }
}